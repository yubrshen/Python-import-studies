from setuptools import setup, find_packages

setup(
    name='python_import_studies',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project's dependencies here.
    ],
)
